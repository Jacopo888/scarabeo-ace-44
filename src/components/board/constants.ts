/**
 * Board UI Constants
 * 
 * Re-esporta SPECIAL_SQUARES da config per uso nei componenti UI.
 * Questa è l'unica fonte di verità per le caselle speciali.
 * 
 * @see src/config/boardConstants.ts per definizione completa
 */
export { 
  SPECIAL_SQUARES, 
  getSpecialSquareType, 
  isSpecialSquare,
  type SpecialSquareType 
} from '@/config/boardConstants'
